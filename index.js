const employeeNames = ['Ada', 'Brendan', 'Ali']
function printBadges(employeeNames) {
  for (let i=0; i < employeeNames.length, i++){
    console.log("Welcome ${employeeNames}! You are employee #[i].")
  }
}
